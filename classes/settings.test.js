const Settings = require("./settings.js");

describe("classes/seettings.js", () => {

  it("should init settings instance", async () => {

    const settings = new Settings();
    // await settings.init();

    // console.log(settings)
    // expect(settings.slotDuration).toBe(50);
  })

});